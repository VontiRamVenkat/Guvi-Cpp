#include <iostream>
using namespace std;
int main()
{
  int a,b,res;
  cout<<"Enter a and b:";
  cin>>a>>b;
  if(a>b)
  {
      res=a-b;
      cout<<res;
  }
  else
  {
      res=a-b;
      cout<<res;
  }
      return 0;
}
